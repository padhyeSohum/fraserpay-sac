
// Export the context hooks and provider from this index file for cleaner imports
export { TransactionProvider, useTransactions } from './TransactionContext';
export type { TransactionContextType } from './types';
